<main class="main">
  <div class="inner">
    <div class="nail__wrapper">
      <p class="nail__hedding">
        <%= @nail.title %>
      </p>
      <%= link_to "by #{@nail.user.nickname}", root_path, class: :nail__user %>
      <% if user_signed_in? && current_user.id == @nail.user_id %>
        <div class="nail__manage">
          <%= link_to "編集する", root_path, class: :nail__btn %>
          <%= link_to "削除する", root_path, class: :nail__btn %>
        </div>
      <% end %>
      <div class="nail__image">
        <%= image_tag @nail.image %>
      </div>
      <div class="nail__body">
        <div class="nail__detail">
          <p class="detail__title">デザインの詳細</p>
          <p class="detail__message">
            <%= @nail.detail %>
          </p>
        </div>
      </div>
      <div class="nail__comments">
        <%# ログインしているユーザーには以下のコメント投稿フォームを表示する %>
          <%# <%= form_with model: モデル名,local: true do |f|%>
            <div class="field">
              <%# <%= f.label :hoge, "コメント" %><br />
              <%# <%= f.text_field :hoge, id:"comment_content" %>
            </div>
            <div class="actions">
              <%# <%= f.submit "送信する", class: :form__btn  %>
            </div>
          <%# <% end %>
        <%# // ログインしているユーザーには上記を表示する %>
        <ul class="comments_lists">
          <%# 投稿に紐づくコメントを一覧する処理を記述する %>
            <li class="comments_list">
              <%# <%= " コメントのテキスト "%>
              <%# <%= link_to "（ ユーザー名 ）", root_path, class: :comment_user %>
            </li>
          <%# // 投稿に紐づくコメントを一覧する処理を記述する %>
        </ul>
      </div>
    </div>
  </div>
</main>
